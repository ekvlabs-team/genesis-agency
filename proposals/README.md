# Proposals

Service, governance and build proposals for the future agency.

These are proposals, not promises.
Do not claim guaranteed income, job flow, token utility or future payouts.

Use `ASSET_PROPOSAL_TEMPLATE.md` for durable asset proposals. Accepted assets
must pass the lifecycle and registry rules in
`../docs/CONTRIBUTION_GOVERNANCE.md`.
